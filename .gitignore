# Operating System Files
.DS_Store
Thumbs.db

# Plan drafts
plan-hub-enlaces*.md

# Configuración personal — nunca se sube (contiene tu nombre, correo y
# enlaces reales). Usa public/config.example.js como plantilla:
#   cp public/config.example.js public/config.js
public/config.js

# Notas/borradores sueltos en la raíz (p. ej. un export de otro CMS que
# uses como material de partida) — no forman parte del proyecto.
/servicios.html

# Sitio estatico independiente de servicios (fuera del control de git de Kardex)
/servicios-standalone/
